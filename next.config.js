/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "qsdfgh6zs9owetzk.public.blob.vercel-storage.com",
      },
    ],
  },
};

module.exports = nextConfig
